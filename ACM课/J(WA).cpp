#include <iostream>
#include <vector>
using namespace std;

int main()
{
    vector<int> vi;
    int m, n;
    cin >> m >> n;
    for (int i = 0; i < m; i++)
    {
        vi.push_back(0);
    }
    while (vi.size() > 1)
    {
    }

    return 0;
}