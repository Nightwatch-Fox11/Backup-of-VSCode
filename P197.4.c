#include <stdio.h>

int main()
{
    char str[100], ch;
    int i = 0, t;
    printf("Enter a message ");
    while ((*(str + i++) = getchar()) != '\n')
        ;
    t = i;

    for (i = 0; i <= (t - 1) / 2; i++)
    {
        if (*(str + i) = *(str))
        {
            /* code */
        }
    }
}