#include <iostream>
#include <cstring>
#include <cstdio>

int main()
{
    using namespace std;
    string a;
    getline(cin, a);
    for (int i = 0; i < a.size(); i++)
    {
        if (a[i] == '%')
        {
            for (int j = 0; j < ; i++)
            {
                /* code */
            }
        }
        else if (a[i] == '.')
        {
            /* code */
        }
        else if (a[i] == '/')
        {
            /* code */
        }
    }

    return 0;
}
void xiaoshu(string a)
{
    for (int i = 0; i < ; i++)
    {
    }
}